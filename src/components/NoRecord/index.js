import React from "react";

const NoRecord = () => {
    return (
        <div className="customer-box customer-details not-found">
            No Record(s) found!
        </div>
    );
};

export default NoRecord;
